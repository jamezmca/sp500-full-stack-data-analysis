//connect with DB in here
const resolvers = {
    Query: {
        getAllUsers() {
            //make return data statement
            return //returns like res.send
        }
    },
    // Mutation: {

    // }
}

module.exports = { resolvers }